/// Shared translations for the YieldPoint mobile apps.
///
/// Both apps take `localizationsDelegates` and `supportedLocales` from
/// [AppLocalizations], so adding a language is one ARB file here rather than
/// one per app.
library flutter_l10n;

export 'l10n/app_localizations.dart';
